<% if mobile_device? %> <!-- Mobile -->

	<p id="notice"><%= notice %></p>
	<%= @group.group_name %>

	 <%= gmaps4rails(@json) %>


<% else %>  <!-- Desktop -->
	<p id="notice"><%= notice %></p>

	<h2><%= @group.group_name %>, <span class="subheading">, by: <%= @group.user.profile_name %></span></h2>

	 
	 <%= gmaps4rails(@json) %>

	


<% end %>